{
  "name": "react-simple-side-effects-monorepo",
  "private": true,
  "scripts": {
    "deploy": "lerna version patch --force-publish --yes && npm run build && lerna publish from-package --yes --contents dist",
    "build": "lerna run build"
  },
  "devDependencies": {
    "@reduxjs/toolkit": "^1.6.1",
    "esbuild": "^0.12.17",
    "lerna": "^4.0.0",
    "rxjs": "^7.3.0",
    "typescript": "^4.3.5"
  }
}
